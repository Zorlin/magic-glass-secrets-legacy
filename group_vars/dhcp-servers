---
# file: group_vars/dhcp-servers

# Chosen DHCP server.
# Valid values are "dnsmasq" and "isc".
dhcp_daemon: dnsmasq

# Set the DHCP range for the DHCP server (or disable it).
# Add a # to the beginning to disable the DHCP server.
dhcp_range: 'dhcp-range=10.0.2.2,10.0.2.102,12h'
